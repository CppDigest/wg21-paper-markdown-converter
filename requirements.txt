# HTML conversion
requests>=2.32
pypandoc>=1.11
pypandoc-binary>=0.1.0
beautifulsoup4>=4.14
lxml>=6.0
python-dotenv>=1.0

# PDF conversion — primary
docling>=2.0

# PDF conversion — fallback 1
pdfplumber>=0.11

# PDF conversion — fallback 2 (OpenRouter Vision)
# PyMuPDF renders PDF pages to images; Pillow encodes them as base64 PNG
pymupdf>=1.24
pillow>=11.0
